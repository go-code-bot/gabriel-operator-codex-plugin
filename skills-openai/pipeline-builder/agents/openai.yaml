interface:
  display_name: "Pipeline Builder"
  short_description: "Draft Gabriel pipelines"
  brand_color: "#2563EB"
  default_prompt: "Use $pipeline-builder to author a local Gabriel pipeline draft without deploying, running, or modifying live records."
