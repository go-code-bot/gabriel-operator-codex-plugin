# Gabriel Digital Twin Embed

This repository controls the public embed appearance for one Gabriel digital twin.

Edit `assets/embed-config.json`, validate it, commit, and push. Gabriel can then pull the file into the live page database projection.
